import type { SoulMeshMessage } from './SoulMeshProtocol'; import { randomUUID } from 'crypto'; import { createSoulMeshNonce, signSoulMeshMessage } from './SoulMeshHmac';
export const NUCLEUS_ID='N06' as const; const PEERS=('N01 N02 N03 N04 N05 N07').split(' ') as Array<'N01'|'N02'|'N03'|'N04'|'N05'|'N07'>; const urls:Partial<Record<string,string>>={N01:process.env.SOUL_MESH_N01_URL,N02:process.env.SOUL_MESH_N02_URL,N03:process.env.SOUL_MESH_N03_URL,N04:process.env.SOUL_MESH_N04_URL,N05:process.env.SOUL_MESH_N05_URL,N07:process.env.SOUL_MESH_N07_URL};
function signedHeaders(message:SoulMeshMessage):Record<string,string>{const headers:Record<string,string>={'content-type':'application/json','accept':'application/json','x-soul-correlation-id':message.correlationId};const secret=process.env.SOUL_MESH_HMAC_SECRET?.trim();if(secret){const nonce=createSoulMeshNonce();message.meta={...(message.meta??{}),nonce};headers['x-soul-mesh-nonce']=nonce;headers['x-soul-mesh-hmac']=signSoulMeshMessage(message,secret,nonce);}return headers;}
export async function sendTo(target:SoulMeshMessage['target'],capability:string,payload:unknown,timeoutMs=15000){const url=urls[target];if(!url)throw new Error(`SOUL_MESH_PEER_URL_NOT_CONFIGURED:${target}`);const correlationId=randomUUID();const message:SoulMeshMessage={protocol:'soul-mesh/1',contractVersion:'1.1.0',id:randomUUID(),correlationId,source:NUCLEUS_ID,target,kind:'request',capability,payload,timestamp:Date.now(),meta:{runtime:'nextjs-ai-chatbot-2000',transport:'HTTP',encoding:'json',version:'1.1.0',traceId:correlationId}};const c=new AbortController();const timer=setTimeout(()=>c.abort(),timeoutMs);try{const response=await fetch(url,{method:'POST',headers:signedHeaders(message),body:JSON.stringify(message),signal:c.signal});const b=await response.json() as SoulMeshMessage;if(b.correlationId!==correlationId)throw new Error('SOUL_MESH_CORRELATION_MISMATCH');if(!response.ok||b.kind==='error')throw new Error(`SOUL_MESH_REMOTE_ERROR:${target}`);return b}finally{clearTimeout(timer)}}
export const N06_OUT_CHANNELS=PEERS.map(x=>`N06.OUT.${x}`); export const N06_IN_CHANNELS=PEERS.map(x=>`N06.IN.${x}`);
