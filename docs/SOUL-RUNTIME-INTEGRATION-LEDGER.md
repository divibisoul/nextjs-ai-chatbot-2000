# N05 Runtime Integration Ledger

Canonical endpoint: `/api/soul-mesh`.

N05 is the orchestration/dispatch nucleus. Its mesh endpoint must invoke the real N05 handler layer and preserve correlation metadata. The N05 role is coordination, not exclusive ownership of all capabilities.

Live E2E connectivity remains deployment-dependent and must be demonstrated with real traffic.
